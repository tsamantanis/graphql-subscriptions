## GraphQL & Apollo Subscriptions Practice

To install dependencies, use: 
```
npm install
```

To run the server, use:
```
npm start
```

Open your browser at `http://localhost:4000` to view the apollo graphql client